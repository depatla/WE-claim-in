const nextConfig = {
  experimental: {
    appDir: true,
    optimizeCss: false,
    optimizePackageImports: false,
  },
  devIndicators: {
    buildActivity: true,
  },
};

module.exports = nextConfig;
